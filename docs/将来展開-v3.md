## 結論
EDAから始める。不動産データもできるだけすべてフェーズでに通わせる

v2大失敗 redis活用指示したのにコーディングをさぼられた

**正解データのデータパイプライン化と継続改善サイクルは、Phase 4 に入れるのが最適**です。
ただし、Phase 3 に **ローカル版の最小ログ収集** を入れ、Phase 4 で **GCS → BigQuery → 評価データ整備 → 再学習候補作成** まで拡張するのが一番きれいです。

つまり、追加するならこうです。

```markdown
Phase 3: Local ハイブリッド検索 + 最小行動ログ収集
Phase 4: GCP MLOps 土台 + 検索ログデータパイプライン + 正解データ整備 + 継続改善サイクル
Phase 7: 完成版として Composer / Vertex / Feature Store / Vector Search / GKE まで統合
```

**新しい Phase を追加する必要は薄い**です。
追加するとしたら Phase 4.5 ですが、学習ロードマップとしては分散しすぎます。Phase 4 の中に **「検索 MLOps データパイプライン」** として入れるのが自然です。

---

## 理由

住宅価格予測では、正解データは比較的シンプルです。

```text
物件特徴量 → 価格予測 → 実際の価格と比較
```

一方、検索では正解データが直接存在しません。

```text
検索クエリ
→ 検索結果表示
→ クリック
→ 詳細閲覧
→ 資料請求
→ 問い合わせ
→ 成約
```

この一連の行動ログから、あとで「この検索結果は良かったのか？」を推定する必要があります。

そのため、検索 MLOps では以下が重要になります。

```text
検索アプリ
→ 行動ログ収集
→ ログ蓄積
→ 正解ラベル作成
→ 評価データ作成
→ リランキング再学習
→ 検索品質評価
→ 再デプロイ
```

これは **GCP MLOps の土台である Phase 4 のテーマとかなり相性が良い**です。

Phase 4 はすでに以下を扱います。

* Cloud Run
* BigQuery
* GCS
* Secret Manager
* Terraform
* WIF
* Cloud Logging

ここに検索ログパイプラインを足すと、単なる「GCP に載せました」ではなく、**検索システムを改善し続ける MLOps** になります。

---

## 有力シナリオ

### シナリオ A：Phase 3 にローカル最小版、Phase 4 に GCP 本番想定版を入れる

これが最有力です。

```markdown
## Phase 3: Local ハイブリッド検索

- Meilisearch
- multilingual-e5
- LightGBM reranker
- Redis
- PostgreSQL
- FastAPI

追加:
- 検索ログ保存
- クリックログ保存
- 資料請求ボタンクリックログ保存
- PostgreSQL に event_log テーブルを作成
- 簡易的な評価データ作成
```

ローカルではこれで十分です。

```text
検索アプリ
→ event_log
→ PostgreSQL
→ 評価用 SQL / Python job
→ LightGBM reranker 再学習用データ
```

この時点では、GCS や BigQuery までは不要です。
Phase 3 の目的は **検索システムの構造理解** なので、ローカル DB で行動ログが取れればよいです。

---

### シナリオ B：Phase 4 で GCP 版の正解データパイプラインを実装する

Phase 4 ではこうします。

```text
検索アプリ
→ Cloud Run
→ 検索ログ / 行動ログ
→ GCS
→ BigQuery
→ 正解データ整備
→ 評価 dataset 作成
→ LightGBM reranker 再学習
→ metrics 保存
```

構成はこれがきれいです。

```text
Cloud Run Search API
  ↓
structured event log
  ↓
GCS raw logs
  ↓
BigQuery events table
  ↓
labeling job
  ↓
BigQuery training dataset
  ↓
LightGBM reranker training
  ↓
metrics table
  ↓
model artifact / registry candidate
```

Phase 4 の学習ポイントに以下を追加します。

```markdown
- 検索ログ / 行動ログの収集
- GCS raw log 保存
- BigQuery へのロード
- クリック / 資料請求 / 問い合わせを使った擬似正解データ作成
- LightGBM reranker 用 training dataset 作成
- オフライン評価指標の保存
- 再学習候補モデルの作成
```

これで Phase 4 がかなり強くなります。
単なる Cloud Run / BigQuery / Terraform ではなく、**検索改善 MLOps の基礎**になります。

---

### シナリオ C：Phase 7 で完成版として Composer 本線に統合する

Phase 7 では、Phase 4 の仕組みを Composer / Vertex / Feature Store / Vector Search に統合します。

```text
Cloud Composer DAG
  ↓
ログ収集確認
  ↓
GCS → BigQuery load
  ↓
labeling
  ↓
feature generation
  ↓
Feature Store 更新
  ↓
Vector Search index 更新
  ↓
LightGBM reranker training
  ↓
Vertex Model Registry 登録
  ↓
evaluation
  ↓
deployment gate
```

Phase 7 は完成版なので、ここでは **本番運用に近い継続改善サイクル** として扱います。

---

## 「クリック」「資料請求ボタンクリック」は正解データか？

結論、**そのままでは正解データではなく、弱い教師信号です。**

かなり重要です。

```text
検索結果表示 = impression
物件クリック = weak positive
詳細ページ滞在 = stronger positive
お気に入り = stronger positive
資料請求ボタンクリック = strong positive
問い合わせ完了 = very strong positive
成約 = strongest positive
即離脱 = weak negative
表示されたが無反応 = weak / noisy negative
```

なので、こう定義するのがよいです。

| 行動           | 正解データとしての強さ | 扱い                   |
| ------------ | ----------: | -------------------- |
| 検索結果に表示      |          なし | impression           |
| 物件クリック       |        弱い正例 | weak positive        |
| 詳細ページ閲覧      |      中程度の正例 | positive             |
| 長時間滞在        |      中程度の正例 | positive             |
| お気に入り        |        強い正例 | strong positive      |
| 資料請求ボタンクリック  |        強い正例 | strong positive      |
| 資料請求完了       |     かなり強い正例 | very strong positive |
| 問い合わせ完了      |     かなり強い正例 | very strong positive |
| 成約           |       最強の正例 | final conversion     |
| 表示されたがクリックなし |        弱い負例 | weak negative        |
| クリック後すぐ離脱    |        弱い負例 | weak negative        |

つまり、資料請求ボタンクリックはかなり有力ですが、厳密にはこうです。

```text
資料請求ボタンクリック = 強い正例候補
資料請求完了 = より強い正例
成約 = 最終正解に近い
```

検索ランキング用には、いきなり成約だけを見るとデータが少なすぎます。
そのため、現実的には以下のような重み付きラベルにします。

```text
click: 1
detail_view: 2
favorite: 3
request_button_click: 4
request_complete: 5
contract: 10
```

LightGBM LambdaRank 用なら、クエリ単位で relevance label を作る形です。

```text
query_id, property_id, relevance_score
```

例：

```text
札幌 1LDK 駅近, property_001, 5
札幌 1LDK 駅近, property_002, 2
札幌 1LDK 駅近, property_003, 0
```

---

## ローカル構成

Phase 3 ではこれで十分です。

```text
検索アプリ
  ↓
検索ログ / クリックログ / 資料請求クリックログ
  ↓
PostgreSQL
  ↓
labeling job
  ↓
training dataset
  ↓
LightGBM reranker 再学習
```

テーブルは最低限これです。

```text
search_events
- search_id
- user_id
- query
- filters
- timestamp

search_impressions
- search_id
- property_id
- rank
- lexical_score
- vector_score
- rerank_score
- timestamp

user_actions
- search_id
- property_id
- action_type
- timestamp
```

`action_type` はこうです。

```text
click
detail_view
favorite
request_button_click
request_complete
inquiry_complete
```

そこから評価データを作ります。

```text
ranking_labels
- query_id
- search_id
- property_id
- relevance_label
```

Phase 3 では、まずここまでで十分価値があります。

---

## GCP / グローバル構成

Phase 4 ではこうします。

```text
Cloud Run Search API
  ↓
structured log / event API
  ↓
GCS raw events
  ↓
BigQuery events
  ↓
BigQuery label tables
  ↓
Cloud Run Job / Python batch
  ↓
LightGBM training dataset
  ↓
model artifact
  ↓
evaluation metrics
```

Phase 4 の到達点はこれです。

```markdown
- 検索行動ログが BigQuery に蓄積される
- BigQuery 上で検索クエリ単位の評価データを作れる
- LightGBM reranker 用の training dataset を作れる
- NDCG@10 / Recall@K / CTR / CVR を保存できる
- 再学習候補モデルを作れる
```

この時点では Vertex Model Registry までは必須にしなくてよいです。
Phase 4 は **GCP MLOps の土台** なので、モデル登録・Feature Store・Vector Search は Phase 5 / 7 側でよいです。

---

## 改訂後の Phase 設計案

以下のように直すのが一番きれいです。

```markdown
### Phase 設計の軸 (改訂版)

- Phase 1: **ML 基礎に集中**  
  学習・評価・保存を扱う。  
  住宅価格予測のように、正解データが比較的明確な回帰問題を通じて、ML の基本サイクルを理解する。  
  **コード化対象**。

- Phase 2: **App / Pipeline / Port-Adapter を導入**  
  アプリケーション、バッチ処理、学習パイプラインを分離し、Port / Adapter・DI・責務分離を導入する。  
  **コード化対象**。

- Phase 3: **不動産検索ドメインで Local ハイブリッド検索を一巡**  
  キーワード検索、ベクトル検索、LightGBM reranker をローカル環境で実装する。  
  さらに、検索ログ・クリックログ・資料請求ボタンクリックログを PostgreSQL に保存し、検索改善に使う行動ログの最小構成を理解する。  
  **コード化対象**。

- Phase 4: **GCP MLOps の土台 + 検索ログデータパイプライン + 継続改善サイクル**  
  Cloud Run / BigQuery / GCS / Secret Manager / Terraform / WIF を用いて、Phase 3 の検索システムを GCP 化する。  
  検索アプリから発生する検索ログ・表示ログ・クリックログ・資料請求ログを GCS / BigQuery に蓄積し、正解データ作成、評価データ整備、LightGBM reranker 再学習、検索品質評価までの継続改善サイクルを構築する。  
  Vertex AI 以降は含めない。  
  **コード化対象**。

- Phase 5: **Vertex AI MLOps 化の技術境界**  
  Phase 7 完成版コードを基準に、Vertex AI Pipelines / Feature Store / Vector Search / Model Registry を理解する。  
  Phase 4 で作成した検索ログ・正解データ・評価データが、Vertex AI 系の MLOps にどう接続されるかを整理する。  
  **論理 Phase（個別コード保守なし）**。

- Phase 6: **PMLE 追加技術 + Composer 本線化の論理境界**  
  Phase 7 完成版コードを基準に、BQML / Dataflow / Explainable AI / Monitoring SLO / Cloud Composer を理解する。  
  検索ログデータパイプラインを Composer DAG 化し、継続的な評価・再学習・監視に接続する。  
  **論理 Phase（個別コード保守なし）**。

- Phase 7: **教材コード完成版 / canonical 起点 / 到達ゴール**  
  Cloud Composer 本線 orchestration + PMLE 4 技術 + Vertex AI Pipelines + Vertex AI Feature Store + Vertex Vector Search + Model Registry + BigQuery + Meilisearch + GKE Deployment + KServe InferenceService をすべて本 phase 配下に本実装する。  
  検索ログ → BigQuery → 正解データ作成 → Feature Store 更新 → Vector Search 更新 → LightGBM reranker 再学習 → 評価 → デプロイ判定までを一連の継続改善サイクルとして扱う。  
  **コード化対象**。
```

---

## 破綻条件

この設計が破綻するのは、主に以下です。

### 1. Phase 3 に詰め込みすぎる

Phase 3 で GCS / BigQuery / Dataflow / Composer まで入れると破綻します。

Phase 3 はあくまでローカルです。

```text
PostgreSQL にログ保存
簡易 labeling
LightGBM reranker 再学習
```

ここまでで止めるべきです。

---

### 2. クリックを「完全な正解」として扱う

これは危険です。

クリックは以下の影響を受けます。

* 表示順位
* 写真の見栄え
* タイトル
* 価格の安さ
* 釣り物件
* ユーザーの偶然
* UI の配置

そのため、クリックは **正解そのものではなく、弱い正解シグナル** として扱うべきです。

---

### 3. 成約だけを正解にする

これも危険です。

成約は最も強い正解に近いですが、データ量が少なすぎます。
学習データとしてはスパースになりやすいです。

したがって、現実的には以下の複合ラベルがよいです。

```text
クリック
詳細閲覧
お気に入り
資料請求クリック
資料請求完了
問い合わせ
成約
```

これらを重み付けして、ランキング学習用の relevance label に変換します。

---

## 実務・行動への影響

この追加はかなり有効です。
理由は、あなたの教材が単なる「検索アプリを作った」から、**検索 MLOps を回せる教材** に変わるからです。

特に案件説明では、以下のように言えるようになります。

```text
不動産検索アプリを題材に、キーワード検索・ベクトル検索・LightGBM reranking を構築。
さらに、検索ログ・クリックログ・資料請求ログを収集し、BigQuery 上で正解データを整備。
NDCG / Recall / CTR / CVR を使って検索品質を評価し、継続的に reranker を改善する MLOps パイプラインを構築しました。
```

これはかなり強いです。

単なる GCP / Vertex / Kubernetes ではなく、以下まで踏み込めます。

```text
検索品質改善
行動ログ設計
正解データ作成
評価データ整備
ランキング学習
MLOps 継続改善
```

最終判断としてはこれです。

```text
Phase 3:
ローカルで検索ログを PostgreSQL に保存する。

Phase 4:
GCP で GCS → BigQuery の検索ログパイプラインを作り、
正解データ作成・評価データ整備・再学習まで入れる。

Phase 7:
Composer / Vertex / Feature Store / Vector Search / GKE まで統合して完成版にする。
```

新 Phase 追加ではなく、**Phase 4 のテーマを拡張する**のが一番合理的です。
